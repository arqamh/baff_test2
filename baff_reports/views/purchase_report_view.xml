<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="document_tax_totals">
        <t t-foreach="tax_totals['subtotals']" t-as="subtotal">
            <tr class="border-black o_subtotal">
                <td>
                    SUBTOTAL
                </td>

                <td class="text-end">
                    <span
                            t-att-class="oe_subtotal_footer_separator"
                            t-esc="subtotal['formatted_amount']"
                    />
                </td>
            </tr>

            <t t-set="subtotal_to_show" t-value="subtotal['name']"/>
            <t t-call="baff_reports.tax_groups_totals"/>
        </t>

        <t t-if="'formatted_rounding_amount' in tax_totals and tax_totals['rounding_amount'] != 0">
            <td>Rounding</td>
            <td class="text-end">
                <span t-esc="tax_totals['formatted_rounding_amount']"/>
            </td>
        </t>

        <!--Total amount with all taxes-->
        <tr class="border-black o_total">
            <td>
                <strong>Discount</strong>
            </td>
            <td class="text-end">
               <span t-field="o.total_discount"/>
            </td>
        </tr>
        <tr class="border-white o_total">
            <td>
                <strong>Total</strong>
            </td>
            <td class="text-end">
                <span t-esc="tax_totals['formatted_amount_total_rounded']"
                      t-if="'formatted_amount_total_rounded' in tax_totals"/>
                <span t-esc="tax_totals['formatted_amount_total']" t-else=""/>
            </td>
        </tr>
    </template>

    <template id="tax_groups_totals">>
        <t t-foreach="tax_totals['groups_by_subtotal'][subtotal_to_show]" t-as="amount_by_group">
            <tr>
                <td>
                    <span t-esc="amount_by_group['tax_group_name']"/>
                    on
                    <t t-esc="amount_by_group['formatted_tax_group_base_amount']"/>
                </td>
                <td class="text-end o_price_total">
                    <span class="text-nowrap" t-esc="amount_by_group['formatted_tax_group_amount']"/>
                </td>
                </tr>
            </t>
    </template>


    <record id="purchase_order_baff_form" model="ir.ui.view">
            <field name="name">purchase_order.baff.form</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <field name="currency_id" position="after">
                    <field name="incoterm_id"/>
                    <field name="incoterm_location"/>
                </field>
            </field>
    </record>
</odoo>