<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="field_required_rule__view__tree" model="ir.ui.view">
        <field name="name">field.required.rule.view.tree</field>
        <field name="model">field.required.rule</field>
        <field name="arch" type="xml">
            <tree string="Field Required Rules"
                  decoration-muted="active == False">
                <field name="name"/>
                <field name="model_id"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <record id="field_required_rule__view__form" model="ir.ui.view">
        <field name="name">field.required.rule.view.form</field>
        <field name="model">field.required.rule</field>
        <field name="arch" type="xml">
            <form string="Field Required Rule">
                <sheet>
                    <widget name="web_ribbon" title="Archived"
                            bg_color="bg-danger"
                            attrs="{'invisible': [('active', '=', True)]}"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name"
                                   placeholder="e.g. Require partner on PO confirmation"/>
                        </h1>
                    </div>
                    <group>
                        <field name="model_id"
                               options="{'no_create': True}"/>
                        <field name="active" invisible="1"/>
                    </group>
                    <separator string="Required Fields"/>
                    <field name="line_ids"
                           attrs="{'invisible': [('model_id', '=', False)]}">
                        <tree editable="bottom">
                            <field name="model_id" invisible="1"/>
                            <field name="trigger_field_id"
                                   options="{'no_create': True}"
                                   domain="[('model_id', '=', model_id),
                                            ('ttype', 'in', ['selection', 'char', 'text', 'html',
                                                             'boolean', 'integer', 'float',
                                                             'monetary', 'many2one'])]"/>
                            <field name="trigger_field_ttype"
                                   string="Type" optional="hide"/>
                            <field name="trigger_values"
                                   placeholder="e.g. purchase,done — or 1,5,8 for IDs / true,false for booleans"/>
                            <field name="field_id"
                                   options="{'no_create': True}"
                                   domain="[('model_id', '=', model_id), ('store', '=', True),
                                            ('compute', '=', False)]"/>
                            <field name="override_group_ids"
                                   string="Override Groups"
                                   widget="many2many_tags"
                                   options="{'no_create': True, 'no_open': True}"/>
                        </tree>
                    </field>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="field_required_rule__view__search" model="ir.ui.view">
        <field name="name">field.required.rule.view.search</field>
        <field name="model">field.required.rule</field>
        <field name="arch" type="xml">
            <search string="Field Required Rules">
                <field name="name"/>
                <field name="model_id"/>
                <filter string="Archived" name="inactive"
                        domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Model" name="group_model"
                            context="{'group_by': 'model_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="field_required_rule__action" model="ir.actions.act_window">
        <field name="name">Field Required Rules</field>
        <field name="res_model">field.required.rule</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id"
               ref="field_required_rule__view__search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Configure per-line required fields based on any field's value.
            </p>
            <p>
                Pick a model, then add one line per required field — each
                line has its own trigger field and trigger values. The
                form/tree will enforce the requirement when the trigger
                matches.
            </p>
        </field>
    </record>

</odoo>
