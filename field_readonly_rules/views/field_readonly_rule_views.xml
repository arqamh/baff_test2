<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="field_readonly_rule__view__tree" model="ir.ui.view">
        <field name="name">field.readonly.rule.view.tree</field>
        <field name="model">field.readonly.rule</field>
        <field name="arch" type="xml">
            <tree string="Field Readonly Rules"
                  decoration-muted="active == False">
                <field name="name"/>
                <field name="model_id"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <record id="field_readonly_rule__view__form" model="ir.ui.view">
        <field name="name">field.readonly.rule.view.form</field>
        <field name="model">field.readonly.rule</field>
        <field name="arch" type="xml">
            <form string="Field Readonly Rule">
                <sheet>
                    <widget name="web_ribbon" title="Archived"
                            bg_color="bg-danger"
                            attrs="{'invisible': [('active', '=', True)]}"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name"
                                   placeholder="e.g. Lock PO on approval"/>
                        </h1>
                    </div>
                    <group>
                        <field name="model_id"
                               options="{'no_create': True}"/>
                        <field name="active" invisible="1"/>
                    </group>
                    <separator string="Fields to Lock"/>
                    <field name="line_ids"
                           attrs="{'invisible': [('model_id', '=', False)]}">
                        <tree editable="bottom">
                            <field name="model_id" invisible="1"/>
                            <field name="trigger_field_origin_id" invisible="1"/>
                            <field name="trigger_field_id"
                                   options="{'no_create': True}"
                                   domain="[('model_id', '=', model_id),
                                            ('ttype', '=', 'selection')]"/>
                            <field name="trigger_selection_ids"
                                   widget="many2many_tags"
                                   options="{'no_create': True, 'no_open': True}"
                                   domain="[('field_id', '=', trigger_field_origin_id)]"/>
                            <field name="field_ids"
                                   widget="many2many_tags"
                                   options="{'no_create': True, 'no_open': True}"
                                   domain="[('model_id', '=', model_id), ('store', '=', True),
                                            ('readonly', '=', False), ('compute', '=', False)]"/>
                            <field name="override_group_ids"
                                   string="Override Groups"
                                   widget="many2many_tags"
                                   options="{'no_create': True, 'no_open': True}"/>
                        </tree>
                    </field>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="field_readonly_rule__view__search" model="ir.ui.view">
        <field name="name">field.readonly.rule.view.search</field>
        <field name="model">field.readonly.rule</field>
        <field name="arch" type="xml">
            <search string="Field Readonly Rules">
                <field name="name"/>
                <field name="model_id"/>
                <filter string="Archived" name="inactive"
                        domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Model" name="group_model"
                            context="{'group_by': 'model_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="field_readonly_rule__action" model="ir.actions.act_window">
        <field name="name">Field Readonly Rules</field>
        <field name="res_model">field.readonly.rule</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id"
               ref="field_readonly_rule__view__search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Configure per-line field locks based on any field's value.
            </p>
            <p>
                Pick a model, then add one line per locked field — each line
                has its own trigger field and trigger values. The form/tree
                will render the configured fields read-only when the
                trigger matches.
            </p>
        </field>
    </record>

</odoo>
