<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Server Action: Reset Landed Cost to Draft -->
    <record id="action_stock_landed_cost_reset_to_draft" model="ir.actions.server">
        <field name="name">Reset to Draft</field>
        <field name="model_id" ref="stock_landed_costs.model_stock_landed_cost"/>
        <field name="binding_model_id" ref="stock_landed_costs.model_stock_landed_cost"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="groups_id" eval="[(4, ref('base.group_system'))]"/>
        <field name="code">
if records:
    records.action_reset_to_draft()
        </field>
    </record>

</odoo>
