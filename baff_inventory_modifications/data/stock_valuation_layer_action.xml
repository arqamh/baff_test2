<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Server Action: Delete Stock Valuation Layer -->
    <record id="action_stock_valuation_layer_delete" model="ir.actions.server">
        <field name="name">Delete Valuation Layer</field>
        <field name="model_id" ref="stock_account.model_stock_valuation_layer"/>
        <field name="binding_model_id" ref="stock_account.model_stock_valuation_layer"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="groups_id" eval="[(4, ref('base.group_system'))]"/>
        <field name="code">
if records:
    records.unlink()
        </field>
    </record>

</odoo>
