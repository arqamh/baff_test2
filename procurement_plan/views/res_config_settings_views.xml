<odoo>
    <record id="res_config_settings_view_form_stock" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.sale.stock.stock</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="stock.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//h2[@id='schedule_info']"  position="before">
                <h2>Styles for Procurement Plan</h2>
                <div class="row mt16 o_settings_container">
                    <div class="col-12 col-lg-12 o_setting_box">
                        <div class="o_setting_right_pane">
                            <label for="procurement_plan_colors"/>
<!--                            <div class="text-muted">-->
<!--                                Select Users to notify when send email from sale order-->
<!--                            </div>-->
                            <div class="content-group">
                                <div class="mt16">
                                    <field name="procurement_plan_colors">
                                        <tree editable="bottom">
                                            <field name="company_id" string="company" invisible="1"/>
                                            <field name="condition_type" string="Condition" />
                                            <field name="condition_colors" string="Color" />
                                            <field name="no_of_dates" string="Day(s)" attrs="{'readonly': [('condition_type', '!=', 'date_before_date')]}" />
                                        </tree>
                                    </field>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>

        </field>
    </record>
</odoo>